<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <script src="https://kit.fontawesome.com/your-font-awesome-kit.js" crossorigin="anonymous"></script>
  <title>Instagram Profile</title>
  <style>
    .post-actions {
      display: flex;
      gap: 1rem;
    }
  </style>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
          <div class="container-fluid">
            <a style="color: inherit;
            text-decoration: none;" class="navbar-brand" href="/home">Connectify</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent" >
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a style="color: inherit;
                  text-decoration: none;" class="nav-link active" aria-current="page" href="/home">Home</a>
                </li>
                <li class="nav-item">
                  <a style="color: inherit;
                  text-decoration: none;" class="nav-link" href="#">Explore</a>
                </li>
                <li class="nav-item">
                  <a style="color: inherit;
                  text-decoration: none;" class="nav-link" href="/addPost">Create Post</a>
                </li>
                <li class="nav-item">
                    <a style="color: inherit;
                    text-decoration: none;" class="nav-link" href="/profile/<%= profile.id %>/edit">Edit Profile</a>
                  </li>
              </ul>
            </div>
          </div>
        </nav>
      </header>
  <div class="container mt-4">
    <div class="row">
      <div class="col-md-4">
        <img src="<%= profile.picture %>" alt="Profile Picture" class="img-fluid rounded-circle">
      </div>
      <div class="col-md-8">
        <h1><%= profile.name %></h1>
        <p>@<%= profile.User.username %></p>
        <ul class="list-inline">
          <li class="list-inline-item"><strong>Followers:</strong> 1000</li>
          <li class="list-inline-item"><strong>Following:</strong> 500</li>
          <li class="list-inline-item"><strong>Posts:</strong> 200</li>
        </ul>
        <p><%= profile.bio %></p>
      </div>
    </div>

    <% profile.User.Posts.forEach(post => { %>
      <div class="row mt-4">
        <div class="col-md-6">
          <img src="<%= post.imageUrl %>" alt="Post Image" class="img-fluid">
        </div>
        <div class="col-md-6">
          <div class="post-actions">    
            
          </div>
          <p class="likes"><%= post.likes %></p>
          <p class="caption"><strong><%= profile.name %></strong> <%= post.content %></p>
          <p class="comments">View all 5 comments</p>
          <p class="time"><%= formatPublished(post.createdAt) %></p>
          <a href="/posts/<%= post.id %>/delete">Delete<i class="far fa-comment"></i></a>
        </div>
      </div>
    <% }) %>
  </div>

  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
